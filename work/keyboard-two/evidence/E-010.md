# E-010
- severity: info
- status: validated
- source_type: local-runtime-code-and-live-status
- repro_command: `swift run protocol-check; swift run sayo-probe --codex-status; ./scripts/package_app.sh; codesign --verify --deep --strict --verbose=2 "dist/Keyboard Studio.app"`
- content_hash: n/a
- linked_workitem: WI-011
- notes: Keyboard Studio 0.3.0 reads recent local rollout JSONL files without modifying them, classifies the last task_started/task_complete/turn_aborted event per task, and caches stable files plus appended bytes. A live read during development reported exactly one active task. Installed-app HID readback showed LED 0 in static RGB 0,104,255 and LED 1 off; unified logs showed only command 0x10 reads/writes and no command 0x04 flash commit. The exact pre-test mode-4 records were restored before the final app relaunch so the in-memory restore baseline is the original rotating animation. Lifecycle and protocol checks pass 38 assertions.
