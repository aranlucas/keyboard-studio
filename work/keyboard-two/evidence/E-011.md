# E-011
- severity: info
- status: validated
- source_type: official-source-live-hardware-native-runtime
- repro_command: `swift build; swift run protocol-check; ./scripts/package_app.sh; codesign --verify --deep --strict --verbose=2 "/Applications/Keyboard Studio.app"; log stream --level info --predicate 'subsystem == "com.lucas.keyboardstudio"'`
- content_hash: n/a
- linked_workitem: WI-012
- notes: The current official SayoDevice configurator and public CLI were used as protocol precedent, then the owned O2L was queried read-only. Live records confirmed two button maps, two Lighting v2 records, six color tables with ten RGB entries each, mutable device name, configured VID/PID, two script-name slots, chunked script image, and 16 one-key text slots. Keyboard Studio 0.4.0 build 7 implements native SwiftUI pages for those surfaces plus opt-in password loading and staged JSON backup/restore. Text/password Save writes only the selected slot. Firmware update, recovery/bootloader, lock, and reset remain guarded rather than guessed. The signed app was installed to /Applications with its stable designated requirement. A post-install unified-log trace showed successful TX/RX for every startup request and no rejected command.
